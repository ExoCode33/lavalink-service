server:
  port: ${SERVER_PORT:2333}
  address: 0.0.0.0

lavalink:
  plugins:
    - dependency: "dev.lavalink.youtube:youtube-plugin:1.5.2"
      repository: "https://maven.lavalink.dev/releases"

  server:
    password: "${LAVALINK_PASSWORD:UtaUtaDj}"
    sources:
      # Audio sources configuration
      youtube: false  # Disabled - handled by YouTube plugin below
      bandcamp: true
      soundcloud: true
      twitch: false
      vimeo: false
      mixer: false
      http: true  # CRITICAL: Enable HTTP for radio streams (DI.FM, etc.)
      local: false
    filters:
      volume: true
      equalizer: true
      karaoke: true
      timescale: true
      tremolo: true
      vibrato: true
      distortion: true
      rotation: true
      channelMix: true
      lowPass: true
    bufferDurationMs: 400
    frameBufferDurationMs: 5000
    trackStuckThresholdMs: 10000
    playerUpdateInterval: 5
    youtubeSearchEnabled: false  # Disabled - handled by YouTube plugin
    soundcloudSearchEnabled: true
    gc-warnings: false
    
    # Resume configuration for better reliability
    resumeKey: "${LAVALINK_RESUME_KEY:uta-dj-bot-railway}"
    resumeTimeout: ${LAVALINK_RESUME_TIMEOUT:60}
    
    ratelimit:
      ipBlocks: []
      excludedIps: []
      strategy: "RotateOnBan"
      searchTriggersFail: true
      retryLimit: -1
      
    # ENHANCED HTTP configuration for streaming services
    httpConfig:
      proxyHost: ""
      proxyPort: 0
      proxyUser: ""
      proxyPassword: ""
      # Optimized for streaming services like DI.FM
      connectTimeoutMs: 20000      # Increased for radio streams
      connectionRequestTimeoutMs: 20000
      socketTimeoutMs: 60000       # Increased for continuous streaming
      maxRedirects: 10             # Allow redirects for radio streams
      # User agent for better compatibility
      userAgent: "Lavalink/4.0.8 (https://github.com/lavalink-devs/Lavalink)"
      
    youtubeConfig:
      email: ""
      password: ""

plugins:
  youtube:
    enabled: ${LAVALINK_PLUGINS_ENABLED:true}
    allowSearch: true
    allowDirectVideoIds: true
    allowDirectPlaylistIds: true
    # Optimized client rotation for better reliability
    clients:
      - MUSIC
      - WEB
      - ANDROID_TESTSUITE
      - WEB_REMIX
      - TVHTML5EMBEDDED
    # Anti-bot detection countermeasures
    countryCode: "${YOUTUBE_COUNTRY_CODE:US}"

metrics:
  prometheus:
    enabled: false
    endpoint: /metrics

sentry:
  dsn: ""
  environment: ""

logging:
  level:
    root: INFO
    lavalink: INFO
    com.sedmelluq.discord.lavaplayer: INFO
    # Enable DEBUG for HTTP stream troubleshooting
    com.sedmelluq.discord.lavaplayer.source.http: DEBUG
    # DI.FM specific logging
    com.sedmelluq.discord.lavaplayer.source.http.HttpAudioSourceManager: DEBUG

  request:
    enabled: true
    includeClientInfo: true
    includeHeaders: false
    includeQueryString: true
    includePayload: true
    maxPayloadLength: 10000

  logback:
    rollingpolicy:
      max-file-size: 25MB
      max-history: 8
